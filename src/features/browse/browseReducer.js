const initialState = {
  categories: {},
};

const browseReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default browseReducer;
